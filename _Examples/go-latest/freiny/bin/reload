#!/bin/bash
home=~
gosrc=$home/go/src
cd $gosrc
ls
rm main
go build -o main
$gosrc/main &
ps -ef
echo "*********************** APP READY"

while inotifywait -e modify $gosrc; do

	killall -9 main
	rm main
	go build -o main
	$gosrc/main &
	ps ef
	echo "*********************** APP READY"

done
